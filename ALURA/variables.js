// variable in Javascript is essentially a container that holds reusable data to be used in the future.
// The significance of the variable is its label. You can change what is inside this box, but not the label.
// We can create a variable using var keyword, followed by the name we want to give to that container. 
// We then use the = sign to assign a value to the variable. var fruit = "apple"; (In this case, we are using the word apple, and placing inside a box called fruit).

// JavaScript Data Types: 
// 1. Primitive: Numbers + Strings + Boolean + Null + Undefined

//The Big 4
// String (words and texts)
// Boolean (True or False)
// Numbers (Integer or float)
// Null (No value)

//2. Reference: Objects + Arrays + Functions

// Tipos Numericos

const age = 28;
const pi = 3.4;

var a = 10
var b = 0
console.log(a/b)
// Infinity (divisao por zero nao eh um erro em JavaScript, ele vai retornar Infinity). 
//Exceto 0/0 nao tem valor bem definido e o resultado sera NaN.

var a = 0
var b = 0
console.log(a/b)
//NaN (see above)
